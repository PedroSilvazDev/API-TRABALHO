services:
  - type: web
    name: api-jogos
    runtime: docker
    dockerfilePath: ./Dockerfile
    # GET que responde 200 — /login é só POST e quebra o health check do Render
    healthCheckPath: /login-test
    plan: free
